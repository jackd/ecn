import kblocks.keras.optimizers.schedules
learning_rate = @tf.keras.optimizers.schedules.ExponentialDecay()
tf.keras.optimizers.schedules.ExponentialDecay.initial_learning_rate = 1e-3
tf.keras.optimizers.schedules.ExponentialDecay.decay_steps = 2000
tf.keras.optimizers.schedules.ExponentialDecay.decay_rate = 0.95

tf.keras.optimizers.Adam.learning_rate = %learning_rate
